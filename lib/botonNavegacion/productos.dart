import 'package:flutter/material.dart';

class productos extends StatelessWidget {
  const productos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('este es el productos'),
    );
  }
}
