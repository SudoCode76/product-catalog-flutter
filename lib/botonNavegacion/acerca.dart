import 'package:flutter/material.dart';

class acerca extends StatelessWidget {
  const acerca({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('este es el acerca'),
    );
  }
}
