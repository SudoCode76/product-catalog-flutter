import 'package:flutter/material.dart';

class activaciones extends StatelessWidget {
  const activaciones({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('esto es activaciones'),
    );
  }
}
